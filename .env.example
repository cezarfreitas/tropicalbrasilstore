# Database Configuration
# Option 1: Full DATABASE_URL (recommended for cloud platforms)
DATABASE_URL=mysql://user:password@host:port/database

# Option 2: Individual MySQL variables (for Docker/local)
MYSQL_HOST=localhost
MYSQL_USER=mysql
MYSQL_PASSWORD=password
MYSQL_DATABASE=default
MYSQL_PORT=3306

# Legacy Docker variables (fallback)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chinelos_store
DB_USER=chinelos_user
DB_PASSWORD=chinelos_pass_2024
DB_ROOT_PASSWORD=chinelos_root_2024

# Application Configuration
PORT=3000
NODE_ENV=production

# Optional: Connection settings
DB_CONNECTION_LIMIT=10

# Application Info
APP_NAME=Chinelos Admin
APP_VERSION=1.0.0

# Optional: SSL Configuration (for production)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem
