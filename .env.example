# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=chinelos_user
DB_PASSWORD=sua_senha_segura_aqui
DB_NAME=chinelos_store
DB_ROOT_PASSWORD=sua_senha_root_aqui

# Alternative: Single Database URL (overrides individual DB_* variables)
# DATABASE_URL=mysql://chinelos_user:sua_senha@localhost:3306/chinelos_store

# Application Configuration
NODE_ENV=production
PORT=3000

# Optional Configuration
DB_CONNECTION_LIMIT=10
APP_NAME="Chinelos Admin"
APP_VERSION="1.0.0"

# Docker Compose Variables (for Easy Panel/Docker deployment)
# Copy the DB_ variables above or set them here specifically for Docker
